# app

React Native (TypeScript) UI: conversation list, chat view, QR pairing, onboarding, mailbox management. All protocol/crypto logic lives in the Rust core (`pm-core`) via `pm-ffi` — this layer is presentation only.

Status: not yet started (M4–M5).
