# Rust
/target/
**/target/
Cargo.lock

# Node / React Native
node_modules/
.expo/
ios/Pods/
ios/build/
android/.gradle/
android/app/build/
# ubrn cross-compiled static libs (100MB+ each per ABI) — rebuild via `npx ubrn build android`
**/jniLibs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env / secrets
.env
.env.local

# Local Claude Code settings
.claude/
