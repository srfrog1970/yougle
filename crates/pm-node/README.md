# pm-node

The Server mailbox binary. Per the PRD (v2.0), this is single-tenant, self-hosted infrastructure a user runs for themselves (e.g., on a Raspberry Pi) — not shared community infrastructure. Handles mailbox writes, fetch, acknowledgment, retention of delivered messages, and the sender-side retry scheduler for reaching Local-only recipients.

Status: not yet started (M2).
